{
	"name": "agentic-usage-monitor",
	"version": "0.0.4",
	"description": "Real-time Claude rate limit monitoring tool for AI coding agents",
	"type": "module",
	"bin": {
		"usage-monitor": "./dist/cli.js",
		"with-monitor": "./bin/with-monitor",
		"usage-monitor-setup": "./bin/setup"
	},
	"files": ["dist", "bin"],
	"scripts": {
		"build": "VERSION=$(node -p \"require('./package.json').version\") && bun build src/cli/index.ts --define \"__PKG_VERSION__=\\\"$VERSION\\\"\" --outfile dist/cli.js --target bun",
		"build:cli": "VERSION=$(node -p \"require('./package.json').version\") && bun build src/cli/index.ts --define \"__PKG_VERSION__=\\\"$VERSION\\\"\" --outfile dist/cli.js --target bun",
		"build:binary": "bun run scripts/build-binary.ts",
		"build:binary:all": "bun run scripts/build-binary.ts --all",
		"cli": "bun run src/cli/index.ts",
		"cli:once": "bun run src/cli/index.ts --once",
		"typecheck": "tsc --noEmit",
		"lint": "biome check .",
		"lint:fix": "biome check --write .",
		"format": "biome format --write .",
		"test": "bun test",
		"test:e2e": "bun test test/e2e",
		"test:e2e:binary": "bun test test/binary-e2e",
		"test:e2e:docker": "docker compose run --rm e2e",
		"test:e2e:report": "mkdir -p test-results && bun test test/e2e 2>&1 | tee test-results/output.txt && bun run test/harness/generate-report.ts",
		"mock-server": "bun run test/mock-server/oauth-server.ts",
		"prepublishOnly": "bun run typecheck && bun run lint && bun run build"
	},
	"keywords": ["opencode", "claude", "anthropic", "rate-limits", "usage", "monitoring"],
	"author": "",
	"license": "MIT",
	"repository": {
		"type": "git",
		"url": "https://github.com/duchangkim/agentic-usage-monitor"
	},
	"dependencies": {
		"zod": "^3.24.0"
	},
	"devDependencies": {
		"@biomejs/biome": "^1.9.0",
		"@types/bun": "latest",
		"typescript": "^5.7.0"
	}
}
